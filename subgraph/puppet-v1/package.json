{
  "name": "gmx-subgraph",
  "license": "MIT",
  "private": true,
  "scripts": {
    "graph-codegen": "pnpm run graph-clean && graph codegen subgraph.arbitrum.yaml",
    "graph-build": "graph build subgraph.arbitrum.yaml",
    "graph-clean": "graph clean",
    "graph-deploy-arbitrum": "graph deploy --node https://api.studio.thegraph.com/deploy/ gmx-house subgraph.arbitrum.yaml",
    "graph-deploy-avalanche": "graph deploy --node https://api.studio.thegraph.com/deploy/ gmx-house subgraph.avalanche.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.64.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}