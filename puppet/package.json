{
  "name": "puppet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ puppet",
    "create-local": "graph create --node http://localhost:8020/ puppet",
    "remove-local": "graph remove --node http://localhost:8020/ puppet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 puppet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.64.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
