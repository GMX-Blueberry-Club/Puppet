import { $text, style } from "@aelea/dom"
import { pallete } from "@aelea/ui-components-theme"

export const $heading1 = $text(style({ fontSize: '2.15rem', fontWeight: '900', marginBottom: '6px', letterSpacing: '.05em', }))
export const $heading2 = $text(style({ fontSize: '1.15rem', fontWeight: '900', marginBottom: '6px', letterSpacing: '.05em', }))